# Public release audit

The public tree contains neutral application code only. A clean installation creates no company, conversation, or knowledge records. All tenant data and runtime state live outside Git in `omnichannel-data`.

The validation commands and current release guarantees are recorded in the repository-root `AUDIT.md`.
